export const Provinces = [
  "AB",
  "BC",
  "MB",
  "NB",
  "NL",
  "NT",
  "NS",
  "NU",
  "ON",
  "PE",
  "QC",
  "SK",
  "YT",
];

export const formValidation = (value, name) => {
  let error;

  switch (name) {
    case "givenName":
      return;
  }
  // return formData.givenName != "" ? formData.surname != "" ?
  // formData.email.split("@") === 2 ? formData.address != "" ?
  // formData.city != "" ? formData.country != "Canada" : true
};
