const intitialState = {};

const paginationReducer = (state = intitialState, action) => {
  switch (action.type) {
    case "ACTION": {
    }
    default:
      return state;
  }
};

export default paginationReducer;
